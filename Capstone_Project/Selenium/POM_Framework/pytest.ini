[pytest]
pythonpath = .
addopts = -v --strict-markers
markers =
    smoke: quick basic checks
    regression: full flow checks
    login: login-related tests
    cart: cart-related tests
    ui: UI interaction (locators/actions/waits)
